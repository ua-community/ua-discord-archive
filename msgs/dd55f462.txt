Been thinking of ditching the web for rendering again. tl;dr, JSDOM + flattening/simplifying the tree (only markdown-level formatting and basic forms) + react-native renderer (also native MacOS/Windows/GNOME)