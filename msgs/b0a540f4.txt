well firstly tor isn't meant for file transfer, so its a bit of a mismatch there considering sharing media is pretty typical for modern social messaging. secondly, its meant to be an *anonymous* network, which is distinct from being a *private* network, so it doesn't make any sense to login in that context. thirdly, there are other new up and coming p2p network frameworks like veilid, like hypercore, like willow, like so many other decentralized networks that someone might want to use but it sounds only to work between two which might not even be all that suitable