Also forced me to make a cleaner split between core and not-core, since anything not-core must be a signed tile. The net result of that is that the command bar and web page host both moved from "features" into core... because they weren't really features, the app can't operate without them and they're each host to multiple extensibility vectors. Simplified things a lot to not pretend they were "separate".