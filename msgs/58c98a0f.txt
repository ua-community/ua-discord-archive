yes thats the long term idea, as any terminal app can also run inside a web terminal it could be part of a graph too. but there are lots of missing parts that we did not even fully plan yet. eg. not sure how the  workflow engine and the connection implementation works for terminal graph do they setup unix pipes or pipe via their runtime, the interface convention could also be ported with something like an adapter