thanks. sure your convo might be helpful!  I am not sure how important extensions are in a future with agents. No extensions are better than limiting or poorly designed ones for open source, because its just so easy to fork and modify directly. This is a really new situation. I would like to push the sec and auth systems as much into the runtime layer as possible so the whole system is working in the same rules. everything else should probably be just UI optimisations.