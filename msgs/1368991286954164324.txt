You made me think of when Service Worker first landed; at the time I was using `SharedWorker` for something. The talk in the office was that `SharedWorker` implementation was a nightmare, and it would be removed imminently in favor of Service Worker.

A decade later, I see `SharedWorker` is still kicking around...